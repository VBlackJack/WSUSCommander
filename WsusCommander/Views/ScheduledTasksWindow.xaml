<Window x:Class="WsusCommander.Views.ScheduledTasksWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:p="clr-namespace:WsusCommander.Properties"
        xmlns:vm="clr-namespace:WsusCommander.ViewModels"
        xmlns:models="clr-namespace:WsusCommander.Models"
        Title="Scheduled Tasks"
        Height="560"
        Width="900"
        MinHeight="450"
        MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <!-- Status Color Converter -->
        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Style.Triggers>
                <DataTrigger Binding="{Binding LastRunStatus}" Value="{x:Static models:TaskExecutionStatus.Success}">
                    <Setter Property="Foreground" Value="#27AE60"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding LastRunStatus}" Value="{x:Static models:TaskExecutionStatus.Warning}">
                    <Setter Property="Foreground" Value="#F39C12"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding LastRunStatus}" Value="{x:Static models:TaskExecutionStatus.Failed}">
                    <Setter Property="Foreground" Value="#E74C3C"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Scheduled Tasks"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="#2C3E50"/>
            <TextBlock Text="Manage automated WSUS operations including staged approvals, cleanup, and synchronization."
                       Foreground="#7F8C8D"
                       TextWrapping="Wrap"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Task List -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Tasks}"
                  SelectedItem="{Binding SelectedTask}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Background="White"
                  BorderThickness="1"
                  BorderBrush="#E0E0E0"
                  RowHeight="36"
                  HeadersVisibility="Column"
                  AutomationProperties.Name="Scheduled Tasks">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F8F9FA"/>
                    <Setter Property="Foreground" Value="#2C3E50"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="12,8"/>
                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="12,8"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E8F4FD"/>
                            <Setter Property="Foreground" Value="#2C3E50"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="White"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                            <Setter Property="Opacity" Value="0.6"/>
                        </DataTrigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E8F4FD"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="" Binding="{Binding IsEnabled, Mode=TwoWay}" Width="40"/>
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name}"
                                    Width="*"/>
                <DataGridTemplateColumn Header="Type" Width="140">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding OperationType, Converter={StaticResource OperationTypeConverter}}"
                                       Padding="12,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Schedule"
                                    Binding="{Binding Schedule.DisplayText}"
                                    Width="160"/>
                <DataGridTextColumn Header="Last Run"
                                    Binding="{Binding LastRunAt, StringFormat={}{0:g}}"
                                    Width="140"/>
                <DataGridTemplateColumn Header="Status" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Style="{StaticResource StatusTextStyle}"
                                       Padding="12,0">
                                <TextBlock.Text>
                                    <Binding Path="LastRunStatus" Converter="{StaticResource StatusConverter}"/>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="New Task"
                    Command="{Binding CreateTaskCommand}"
                    Background="#3498DB"
                    Foreground="White"
                    Padding="16,8"
                    MinWidth="120"
                    Margin="0,0,8,0"/>
            <Button Content="Edit"
                    Command="{Binding EditTaskCommand}"
                    IsEnabled="{Binding SelectedTask, Converter={StaticResource NullToBoolConverter}}"
                    Background="#95A5A6"
                    Foreground="White"
                    Padding="16,8"
                    MinWidth="100"
                    Margin="0,0,8,0"/>
            <Button Content="Delete"
                    Command="{Binding DeleteTaskCommand}"
                    IsEnabled="{Binding SelectedTask, Converter={StaticResource NullToBoolConverter}}"
                    Background="#E74C3C"
                    Foreground="White"
                    Padding="16,8"
                    MinWidth="100"
                    Margin="0,0,8,0"/>
            <Button Content="Run Now"
                    Command="{Binding RunTaskNowCommand}"
                    IsEnabled="{Binding SelectedTask, Converter={StaticResource NullToBoolConverter}}"
                    Background="#27AE60"
                    Foreground="White"
                    Padding="16,8"
                    MinWidth="120"
                    Margin="0,0,8,0"/>
        </StackPanel>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Refresh"
                    Command="{Binding RefreshCommand}"
                    Padding="16,8"
                    MinWidth="100"
                    Margin="0,0,8,0"/>
            <Button Content="Close"
                    Click="CloseButton_Click"
                    Padding="16,8"
                    MinWidth="100"/>
        </StackPanel>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="4"
                Background="#80FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                <TextBlock Text="Loading..."
                           Margin="0,8,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
