<Window x:Class="WsusCommander.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:p="clr-namespace:WsusCommander.Properties"
        Title="{Binding Title, RelativeSource={RelativeSource Self}}"
        Height="160"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F5F5F5"
        FocusManager.FocusedElement="{Binding ElementName=InputTextBox}">

    <Window.Resources>
        <!-- Focus Visual Style -->
        <Style x:Key="DialogFocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Stroke="#FF6B00" StrokeThickness="2"
                                   RadiusX="4" RadiusY="4" Margin="-2"
                                   SnapsToDevicePixels="True"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dialog Button Style -->
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{StaticResource DialogFocusVisual}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Prompt -->
        <TextBlock Grid.Row="0"
                   x:Name="PromptText"
                   Text="{Binding Prompt, RelativeSource={RelativeSource AncestorType=Window}}"
                   FontSize="13"
                   Foreground="#2C3E50"
                   Margin="0,0,0,10"/>

        <!-- Input TextBox -->
        <TextBox Grid.Row="1"
                 x:Name="InputTextBox"
                 Text="{Binding InputValue, RelativeSource={RelativeSource AncestorType=Window}, UpdateSourceTrigger=PropertyChanged}"
                 Padding="8,6"
                 FontSize="13"
                 Margin="0,0,0,15"
                 TabIndex="0"
                 AutomationProperties.LabeledBy="{Binding ElementName=PromptText}">
            <TextBox.InputBindings>
                <KeyBinding Key="Return" Command="{Binding OkCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                <KeyBinding Key="Escape" Command="{Binding CancelCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
            </TextBox.InputBindings>
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TextBox">
                                <Border x:Name="border"
                                        Background="White"
                                        BorderBrush="#E0E0E0"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="{TemplateBinding Padding}">
                                    <ScrollViewer x:Name="PART_ContentHost" Focusable="False"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter TargetName="border" Property="BorderBrush" Value="#FF6B00"/>
                                        <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TextBox.Style>
        </TextBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="{x:Static p:Resources.BtnOk}"
                    Click="OkButton_Click"
                    Background="#3498DB"
                    Style="{StaticResource DialogButtonStyle}"
                    Margin="0,0,10,0"
                    TabIndex="1"
                    IsDefault="True"
                    AutomationProperties.Name="{x:Static p:Resources.BtnOk}"/>
            <Button Content="{x:Static p:Resources.BtnCancel}"
                    Click="CancelButton_Click"
                    Background="#6C757D"
                    Style="{StaticResource DialogButtonStyle}"
                    TabIndex="2"
                    IsCancel="True"
                    AutomationProperties.Name="{x:Static p:Resources.BtnCancel}"/>
        </StackPanel>
    </Grid>
</Window>
